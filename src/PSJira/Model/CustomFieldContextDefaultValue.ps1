#
# The Jira Cloud platform REST API
# Jira Cloud platform REST API documentation
# Version: 1001.0.0-SNAPSHOT-23ad6c5456aade2d59a4e9437ce49c945df2eb8f
# Contact: ecosystem@atlassian.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

CustomFieldContextDefaultValue<PSCustomObject>
#>
function ConvertFrom-JsonToCustomFieldContextDefaultValue {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match CustomFieldContextDefaultValueCascadingOption defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueCascadingOption $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueCascadingOption"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueCascadingOption' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueDate defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueDate $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueDate"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueDate' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueDateTime defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueDateTime $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueDateTime"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueDateTime' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueFloat defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueFloat $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueFloat"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueFloat' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueForgeDateTimeField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueForgeDateTimeField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueForgeDateTimeField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueForgeDateTimeField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueForgeGroupField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueForgeGroupField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueForgeGroupField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueForgeGroupField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueForgeMultiGroupField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueForgeMultiGroupField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueForgeMultiGroupField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueForgeMultiGroupField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueForgeMultiStringField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueForgeMultiStringField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueForgeMultiStringField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueForgeMultiStringField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueForgeMultiUserField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueForgeMultiUserField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueForgeMultiUserField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueForgeMultiUserField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueForgeNumberField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueForgeNumberField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueForgeNumberField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueForgeNumberField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueForgeObjectField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueForgeObjectField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueForgeObjectField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueForgeObjectField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueForgeStringField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueForgeStringField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueForgeStringField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueForgeStringField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueForgeUserField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueForgeUserField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueForgeUserField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueForgeUserField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueLabels defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueLabels $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueLabels"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueLabels' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueMultiUserPicker defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueMultiUserPicker $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueMultiUserPicker"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueMultiUserPicker' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueMultipleGroupPicker defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueMultipleGroupPicker $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueMultipleGroupPicker"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueMultipleGroupPicker' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueMultipleOption defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueMultipleOption $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueMultipleOption"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueMultipleOption' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueMultipleVersionPicker defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueMultipleVersionPicker $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueMultipleVersionPicker"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueMultipleVersionPicker' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueProject defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueProject $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueProject"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueProject' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueReadOnly defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueReadOnly $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueReadOnly"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueReadOnly' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueSingleGroupPicker defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueSingleGroupPicker $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueSingleGroupPicker"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueSingleGroupPicker' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueSingleOption defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueSingleOption $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueSingleOption"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueSingleOption' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueSingleVersionPicker defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueSingleVersionPicker $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueSingleVersionPicker"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueSingleVersionPicker' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueTextArea defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueTextArea $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueTextArea"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueTextArea' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueTextField defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueTextField $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueTextField"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueTextField' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextDefaultValueURL defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextDefaultValueURL $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextDefaultValueURL"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextDefaultValueURL' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        # try to match CustomFieldContextSingleUserPickerDefaults defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToCustomFieldContextSingleUserPickerDefaults $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "CustomFieldContextSingleUserPickerDefaults"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'CustomFieldContextSingleUserPickerDefaults' defined in oneOf (CustomFieldContextDefaultValue). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([CustomFieldContextDefaultValueCascadingOption, CustomFieldContextDefaultValueDate, CustomFieldContextDefaultValueDateTime, CustomFieldContextDefaultValueFloat, CustomFieldContextDefaultValueForgeDateTimeField, CustomFieldContextDefaultValueForgeGroupField, CustomFieldContextDefaultValueForgeMultiGroupField, CustomFieldContextDefaultValueForgeMultiStringField, CustomFieldContextDefaultValueForgeMultiUserField, CustomFieldContextDefaultValueForgeNumberField, CustomFieldContextDefaultValueForgeObjectField, CustomFieldContextDefaultValueForgeStringField, CustomFieldContextDefaultValueForgeUserField, CustomFieldContextDefaultValueLabels, CustomFieldContextDefaultValueMultiUserPicker, CustomFieldContextDefaultValueMultipleGroupPicker, CustomFieldContextDefaultValueMultipleOption, CustomFieldContextDefaultValueMultipleVersionPicker, CustomFieldContextDefaultValueProject, CustomFieldContextDefaultValueReadOnly, CustomFieldContextDefaultValueSingleGroupPicker, CustomFieldContextDefaultValueSingleOption, CustomFieldContextDefaultValueSingleVersionPicker, CustomFieldContextDefaultValueTextArea, CustomFieldContextDefaultValueTextField, CustomFieldContextDefaultValueURL, CustomFieldContextSingleUserPickerDefaults]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("CustomFieldContextDefaultValueCascadingOption", "CustomFieldContextDefaultValueDate", "CustomFieldContextDefaultValueDateTime", "CustomFieldContextDefaultValueFloat", "CustomFieldContextDefaultValueForgeDateTimeField", "CustomFieldContextDefaultValueForgeGroupField", "CustomFieldContextDefaultValueForgeMultiGroupField", "CustomFieldContextDefaultValueForgeMultiStringField", "CustomFieldContextDefaultValueForgeMultiUserField", "CustomFieldContextDefaultValueForgeNumberField", "CustomFieldContextDefaultValueForgeObjectField", "CustomFieldContextDefaultValueForgeStringField", "CustomFieldContextDefaultValueForgeUserField", "CustomFieldContextDefaultValueLabels", "CustomFieldContextDefaultValueMultiUserPicker", "CustomFieldContextDefaultValueMultipleGroupPicker", "CustomFieldContextDefaultValueMultipleOption", "CustomFieldContextDefaultValueMultipleVersionPicker", "CustomFieldContextDefaultValueProject", "CustomFieldContextDefaultValueReadOnly", "CustomFieldContextDefaultValueSingleGroupPicker", "CustomFieldContextDefaultValueSingleOption", "CustomFieldContextDefaultValueSingleVersionPicker", "CustomFieldContextDefaultValueTextArea", "CustomFieldContextDefaultValueTextField", "CustomFieldContextDefaultValueURL", "CustomFieldContextSingleUserPickerDefaults")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([CustomFieldContextDefaultValueCascadingOption, CustomFieldContextDefaultValueDate, CustomFieldContextDefaultValueDateTime, CustomFieldContextDefaultValueFloat, CustomFieldContextDefaultValueForgeDateTimeField, CustomFieldContextDefaultValueForgeGroupField, CustomFieldContextDefaultValueForgeMultiGroupField, CustomFieldContextDefaultValueForgeMultiStringField, CustomFieldContextDefaultValueForgeMultiUserField, CustomFieldContextDefaultValueForgeNumberField, CustomFieldContextDefaultValueForgeObjectField, CustomFieldContextDefaultValueForgeStringField, CustomFieldContextDefaultValueForgeUserField, CustomFieldContextDefaultValueLabels, CustomFieldContextDefaultValueMultiUserPicker, CustomFieldContextDefaultValueMultipleGroupPicker, CustomFieldContextDefaultValueMultipleOption, CustomFieldContextDefaultValueMultipleVersionPicker, CustomFieldContextDefaultValueProject, CustomFieldContextDefaultValueReadOnly, CustomFieldContextDefaultValueSingleGroupPicker, CustomFieldContextDefaultValueSingleOption, CustomFieldContextDefaultValueSingleVersionPicker, CustomFieldContextDefaultValueTextArea, CustomFieldContextDefaultValueTextField, CustomFieldContextDefaultValueURL, CustomFieldContextSingleUserPickerDefaults]). JSON Payload: $($Json)"
        }
    }
}

