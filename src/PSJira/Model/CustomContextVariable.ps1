#
# The Jira Cloud platform REST API
# Jira Cloud platform REST API documentation
# Version: 1001.0.0-SNAPSHOT-23ad6c5456aade2d59a4e9437ce49c945df2eb8f
# Contact: ecosystem@atlassian.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

No description available.

.PARAMETER Json

JSON object

.OUTPUTS

CustomContextVariable<PSCustomObject>
#>
function ConvertFrom-JsonToCustomContextVariable {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match IssueContextVariable defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToIssueContextVariable $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "IssueContextVariable"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'IssueContextVariable' defined in oneOf (CustomContextVariable). Proceeding to the next one if any."
        }

        # try to match JsonContextVariable defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToJsonContextVariable $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "JsonContextVariable"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'JsonContextVariable' defined in oneOf (CustomContextVariable). Proceeding to the next one if any."
        }

        # try to match UserContextVariable defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToUserContextVariable $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "UserContextVariable"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'UserContextVariable' defined in oneOf (CustomContextVariable). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([IssueContextVariable, JsonContextVariable, UserContextVariable]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("IssueContextVariable", "JsonContextVariable", "UserContextVariable")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([IssueContextVariable, JsonContextVariable, UserContextVariable]). JSON Payload: $($Json)"
        }
    }
}

